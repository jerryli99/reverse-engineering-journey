#include "mylib.h"

int add(int a, int b) {
    return (a + b);
}

int minus(int a, int b) {
    return (a - b + 100);
}

int multiply(int a, int b) {
    return (a*b*2);
}

int divide(int a, int b) {
    return (a / b);
}

